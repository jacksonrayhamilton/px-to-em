{
  "name": "px-to-em",
  "description": "Convert a px value to an em value",
  "version": "1.0.1",
  "main": "pxToEm.mjs",
  "scripts": {
    "build": "rollup -c",
    "develop": "rollup -c -w"
  },
  "devDependencies": {
    "rollup": "^0.50.0",
    "rollup-plugin-shift-header": "^1.0.1"
  }
}
